import { z } from 'zod';
export const gameConfigSchema = z.object({ rounds: z.array(z.object({ roundNumber: z.number(), fields: z.array(z.object({ key: z.string(), label: z.string(), type: z.enum(['number','select']), min: z.number().optional(), max: z.number().optional(), options: z.array(z.object({label:z.string(), value:z.string()})).optional(), required: z.boolean().default(true) })), scoring: z.object({ weights: z.record(z.string(), z.number()).default({}), bonusIf: z.object({ field: z.string(), equals: z.string(), amount: z.number() }).optional() }), unlockRules: z.object({ minPriorScore: z.number().optional() }).optional() })) });
